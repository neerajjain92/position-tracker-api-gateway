spring:
  application:
    name: gateway
    port: 8080

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

management:
  endpoints:
    web:
      exposure:
        include: '*'

position-tracker-url: http://localhost:8090
